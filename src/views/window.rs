use crate::WidgetIds;
use conrod::*;

pub struct WindowView {
}

impl WindowView {
    pub fn new() -> WindowView {
        WindowView {
        }
    }

    pub fn display_background(&self, ui: &mut UiCell, widget_ids: &WidgetIds) {
        
    }
}